# Testing Commands for raiseyourvoice.co.in Domain Setup
# Snowflake Account: GMOFVVK-QA77419
# App: TweeterBot
# Domain: raiseyourvoice.co.in

## 1. Test DNS Resolution
# Check if your domain points to the correct Snowflake server
nslookup raiseyourvoice.co.in
nslookup www.raiseyourvoice.co.in

# Check specific Snowflake server
nslookup GMOFVVK-QA77419.snowflakecomputing.com

## 2. Test HTTP/HTTPS Access
# Test HTTP (should redirect to HTTPS)
curl -I http://raiseyourvoice.co.in
curl -I http://www.raiseyourvoice.co.in

# Test HTTPS
curl -I https://raiseyourvoice.co.in
curl -I https://www.raiseyourvoice.co.in

# Test redirect to Snowflake app
curl -L https://raiseyourvoice.co.in

## 3. Test SSL Certificate
# Check SSL certificate validity
openssl s_client -connect raiseyourvoice.co.in:443 -servername raiseyourvoice.co.in

# Check certificate details
echo | openssl s_client -connect raiseyourvoice.co.in:443 -servername raiseyourvoice.co.in 2>/dev/null | openssl x509 -noout -text

## 4. Test App Functionality
# Open in browser and test:
# 1. https://raiseyourvoice.co.in
# 2. Upload an image
# 3. Generate a tweet
# 4. Check if all features work

## 5. Performance Testing
# Test loading speed
curl -w "@curl-format.txt" -o /dev/null -s https://raiseyourvoice.co.in

# Create curl-format.txt with:
#      time_namelookup:  %{time_namelookup}\n
#         time_connect:  %{time_connect}\n
#      time_appconnect:  %{time_appconnect}\n
#     time_pretransfer:  %{time_pretransfer}\n
#        time_redirect:  %{time_redirect}\n
#   time_starttransfer:  %{time_starttransfer}\n
#                      ----------\n
#           time_total:  %{time_total}\n

## 6. Mobile Testing
# Test on mobile devices:
# - iPhone Safari
# - Android Chrome
# - Check responsive design

## 7. Troubleshooting Commands
# Check if domain resolves
dig raiseyourvoice.co.in
dig www.raiseyourvoice.co.in

# Check HTTP headers
curl -I -H "User-Agent: Mozilla/5.0" https://raiseyourvoice.co.in

# Check for mixed content issues
curl -s https://raiseyourvoice.co.in | grep -i "http://"

## 8. Expected Results
# DNS Resolution: Should point to Snowflake IP
# HTTPS: Should work without certificate errors
# Redirect: Should redirect to Snowflake app
# App: Should load and function normally
# Mobile: Should work on all devices

## 9. Common Issues and Solutions
# Issue: Domain not resolving
# Solution: Wait 24-48 hours for DNS propagation

# Issue: SSL certificate errors
# Solution: Check Cloudflare SSL settings or Let's Encrypt setup

# Issue: App not loading
# Solution: Check proxy configuration and Snowflake app status

# Issue: Mixed content warnings
# Solution: Ensure all resources use HTTPS
